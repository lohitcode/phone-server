# Desktop metadata
.DS_Store

# Local credentials — never commit
/.env

# camera test output (phone-snap.sh)
/remote.jpg

# Go build output
/bin/

# SQLite DB files / backups — never commit data
*.db
*.db-wal
*.db-shm
/backups/
